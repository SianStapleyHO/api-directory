---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .NAME }}
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: {{ .NAME }}
              servicePort: 8080
